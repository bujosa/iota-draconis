'use strict'

module.exports.findPlanetByname = function findPlanetByname(req, res, next) {
  res.send({
    message: 'This is the mockup controller for findPlanetByname'
  });
};

module.exports.deletePlanet = function deletePlanet(req, res, next) {
  res.send({
    message: 'This is the mockup controller for deletePlanet'
  });
};

module.exports.updatePlanet = function updatePlanet(req, res, next) {
  res.send({
    message: 'This is the mockup controller for updatePlanet'
  });
};