'use strict'

module.exports.getPlanets = function getPlanets(req, res, next) {
  res.send({
    message: 'This is the mockup controller for getPlanets'
  });
};

module.exports.addPlanet = function addPlanet(req, res, next) {
  res.send({
    message: 'This is the mockup controller for addPlanet'
  });
};